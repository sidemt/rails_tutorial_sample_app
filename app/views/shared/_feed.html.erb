<% if @feed_items.any? %>
  <ol class="microposts">
    <%# @feed_itemsの各アイテムがMicropostクラスなので、
    app/views/microposts/_micropost.html.erb が使われる %>
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
